---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";
import createSlug from "../lib/createSlug"

const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-5xl font-bold">Sulkyoptimism</div>
    <div class="text-3xl py-3 font-bold">Just someone who does stuff</div>
    <div class="py-2">
      <text class="text-lg">
        This is a space for recording projects, tracking progress and generally just a think-pad.
        Typically I never write anything down, my mantra has always been:
      </text>
    </div>
    <div class="p-2">
      <text class="text-xl">
        If its interesting then you <b>learn</b> it, if you learn it you <b>know</b> it; things you know, you dont need to remember.
      </text>
    </div>
    <div class="py-2">
      <text class="text-lg">
        But I wanted to try something different, and so <i> here we are.</i>
      </text>
    </div>
    <div class="mt-8">
      <div class="py-2">I also work on a large project with a few others called Risk of Resources, go check it out!</div>
      <a class="btn" href="https://www.riskofresources.com/" target="_blank">Risk of Resources</a>
      <a class="btn btn-outline" href="/projects/">Write up</a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">My last projects {"</>"}</div>
  </div>

  <HorizontalCard
    title="Skating Light set up"
    img="/post_img.webp"
    desc="My current personal indulgence project"
    url="/projects/skate-lighting-insert-intro"
    badge="NEW"
    target="_self"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Risk of Resources"
    img="/post_img.webp"
    desc="A large project specifically made to facilitate events in the RoR2 twitch community."
    url="/projects/risk-of-resources"
    target="_self"
  />
</BaseLayout>
